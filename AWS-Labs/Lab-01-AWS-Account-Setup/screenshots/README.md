# Required Screenshots

1. AWS account security settings with sensitive values hidden
2. MFA or IAM identity setup
3. `aws sts get-caller-identity` validation with account details redacted
4. Configured AWS Region
5. Final validation result

Do not include credentials, access keys, account IDs, email addresses, or other sensitive data.
